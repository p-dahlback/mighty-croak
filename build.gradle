apply plugin: 'java'

sourceCompatibility = 1.6
version = '1.0'

repositories {
    mavenCentral()
}

allprojects {
    version = '1.1'

    ext {
        gdxVersion = "1.3.1"
    }
}

dependencies {

    testCompile group: 'junit', name: 'junit', version: '4.11'
}